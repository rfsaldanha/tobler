home_tab <- argonTabItem(
  tabName = "home",
  argonRow(
    center = TRUE,
    argonColumn(
      width = 6,
      argonImage(
        src = "tobleR.png",
        floating = TRUE,
        card_mode = FALSE 
      )
    )
  )
)